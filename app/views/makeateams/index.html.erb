<h1>Listing makeateams</h1>

<table>
  <tr>
    <th>Info</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @makeateams.each do |makeateam| %>
  <tr>
    <td><%= makeateam.info %></td>
    <td><%= link_to 'Show', makeateam %></td>
    <td><%= link_to 'Edit', edit_makeateam_path(makeateam) %></td>
    <td><%= link_to 'Destroy', makeateam, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Makeateam', new_makeateam_path %>
